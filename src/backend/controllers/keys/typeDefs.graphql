type Key {
  _id: ID!
  deviceName: String!
  publicKey: String!
  user: User!
  userId: ID!
  config: String
  info: PeerInfo
}

type PeerInfo {
  ip: String
  time: Int
}

extend type Query {
  current_user_keys: [Key]!
}

extend type Mutation {
  create_key(deviceName: String!): Key
}
